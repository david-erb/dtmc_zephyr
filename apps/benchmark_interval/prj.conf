# see DTMC_ZEPHYR_SYSTEM_COMMON.conf included by CMakeLists.txt

# counter driver required by dtinterval_zephyr_counter
CONFIG_COUNTER=y

# DWT CYCCNT halts during WFI; disable PM so cycle measurements stay valid
CONFIG_PM=n
